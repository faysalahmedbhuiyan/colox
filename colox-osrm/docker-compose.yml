version: '3.8'

services:
  osrm-routing:
    image: osrm/osrm-backend
    container_name: colox-osrm
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./data:/data
    command: osrm-routed --algorithm mld /data/bangladesh-latest.osrm